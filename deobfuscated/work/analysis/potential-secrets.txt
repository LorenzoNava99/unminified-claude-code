    apiKeyFromFd: undefined,
  return YB.apiKeyFromFd;
  YB.apiKeyFromFd = A;
    if (Z.username || Z.password) {
      G.auth = `${Z.username}:${Z.password}`;
      TG(this, zMA, (B == null ? undefined : B.withCredentials) ?? false);
    get withCredentials() {
      B.credentials = this.withCredentials ? "include" : "same-origin";
    client_secret: k.string().optional(),
    client_secret_expires_at: k.number().optional()
  let Q = A.client_secret !== undefined;
      return "client_secret_post";
  if (Q && B.includes("client_secret_basic")) {
    return "client_secret_basic";
  if (Q && B.includes("client_secret_post")) {
    return "client_secret_post";
    return "client_secret_post";
    client_secret: Z
    case "client_secret_basic":
    case "client_secret_post":
    throw Error("client_secret_basic authentication requires a client_secret");
    Q.set("client_secret", B);
      await ((Q = A.invalidateCredentials) === null || Q === undefined ? undefined : Q.call(A, "all"));
      await ((I = A.invalidateCredentials) === null || I === undefined ? undefined : I.call(A, "tokens"));
  RX9.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = OX9;
      I.auth = (I.username || "") + ":" + (I.password || "");
      if (I.auth.username || I.auth.password) {
        I.auth = (I.auth.username || "") + ":" + (I.auth.password || "");
        let DA = B.auth.password || "";
          password: DA
